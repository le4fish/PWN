用途：
在没有启动 PIE，canary 的文件里，栈从高往低存储，从低到高执行

输入的字符串超出数组的极限，且其中没有 '0x00' 字符，则可以造成栈溢出

修改返回地址，让其指向m内存中已有的某个函数

方法：
存储的代码和栈桢存储子函数类似，先参数 '/bin/sh'，然后返回地址 padding2，再是调用函数地址精准覆盖返回地址，最后精准覆盖基地址（也可以没有）

payload:padding1 + address of  system() + padding2 + address of '/bin/sh'

注释：
为了攻破栈空间不可执行，我们返回程序中原有函数来执行，pwn题中，最常用的就是系统函数的返回。
