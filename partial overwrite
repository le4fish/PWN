这是一种stack canary泄露的办法。

首先，canary在内核中读取FS的值，这数值是不可知的（cancary有很多种方法去得到），且在函数返回、基地址之前，异或xor验证。

且canary为了防止其他漏洞，字符串最后一位是'\x00'，小端序存储在开头。

有条件的话，可以输入数组多一位，然后输出时可以得到除最后一位的完整canary。

注意，因为canary已经修改，所以此payload不能循环调用。
